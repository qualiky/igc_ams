'use strict';

/**
 * yearly-leave-detail service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::yearly-leave-detail.yearly-leave-detail');
