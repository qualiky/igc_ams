'use strict';

/**
 * yearly-leave-detail router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::yearly-leave-detail.yearly-leave-detail');
